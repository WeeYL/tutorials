_______Header______("testing");

type k = string;
type v = number;


// header
function _______Header______(params:string) {
  params = params.toUpperCase();
  console.log(`--------------- ${params} `);
}
